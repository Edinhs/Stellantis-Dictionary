# backend — API REST (Node.js + TypeScript)

Pacote do servidor. Health-check e infra de container são definidos junto ao
baseline de DevOps (`docker/`, `.env.example`, CI). Ver `src/README.md` e
`docs/13-arquitetura-modular.md`.

Scripts previstos (placeholders — serão definidos na implementação): `lint`,
`test`, `build`, `dev`. Configuração só via ambiente (`.env`, ver `.env.example`).
