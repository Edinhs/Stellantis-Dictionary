# tests — Testes por módulo

`tests/backend/` espelha `backend/src/modules/` (um diretório de teste por
módulo) para testabilidade e auditoria por feature; `tests/frontend/` cobre a
SPA. Estrutura definida agora; casos entram junto com cada módulo (ver backlog
`04-tasks.md`). Rodam na CI (lint/testes/build).
